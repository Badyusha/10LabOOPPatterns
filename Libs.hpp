#ifndef LIBS_HPP
#define LIBS_HPP

#include <iostream>
#include <vector>
#include <algorithm>
#include <memory>
#include <Windows.h>
#include <string>

enum PRODUCTS {
	GEO_MAP,
	BOOK,
	NEWSPAPER,
	NOTEBOOK,
	MAGAZINE
};

#endif